#ifndef MESSAGEOBJECT_H
#define MESSAGEOBJECT_H

#include <QObject>
#include "autogenerated/types/types.h"

class MessageObject : public QObject
{
    Q_OBJECT

    Q_PROPERTY(User* fromUser READ fromUser NOTIFY fromUserChanged)
    Q_PROPERTY(QString messageText READ messageText NOTIFY messageTextChanged)

    public:
        explicit MessageObject(QObject *parent = 0);
        Message* message() const;
        User* fromUser() const;
        QString messageText() const;
        void setMessage(Message* message);
        void setFromUser(User* fromuser);

    signals:
        void messageChanged();
        void fromUserChanged();
        void messageTextChanged();

    private:
        User* _fromuser;
        Message* _message;
};

#endif // MESSAGEOBJECT_H
